import React from 'react';
import { StyleSheet, Text, View, SafeAreaView } from 'react-native';

const Profile = ({ navigation }) => {
    return(
        <SafeAreaView style={{flex: 1}}>
            <View>
                <Text>ProfileSlaps</Text>
            </View>
        </SafeAreaView>
    );
}

export default Profile;