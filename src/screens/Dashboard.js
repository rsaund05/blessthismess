import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const Dashboard = ({ navigaton }) => {
    return(
        <View>
            <Text>DASHBOARD_TEMP_DELETE</Text>
        </View>
    );
};

export default Dashboard;