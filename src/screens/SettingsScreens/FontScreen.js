import React from 'react';
import { StyleSheet, Text, View, SafeAreaView } from 'react-native';

const FontScreen = ({ navigation }) => {
    return(
        <SafeAreaView style={{flex: 1}}>
            <View>
                <Text>FontScreen</Text>
            </View>
        </SafeAreaView>
    );
};

export default FontScreen;